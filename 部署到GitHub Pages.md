# 英语萌宠乐园 · 部署到 GitHub Pages

全程约 15 分钟，只需要做一次。做完之后平板就有专属链接，添加到主屏后像一个 APP。

---

## 一、准备工作

### 1. 注册/登录 GitHub
打开 https://github.com ，用你的账号登录。

### 2. 本文件夹需要上传的文件

```
ket-park/
├── index.html          ← 主程序
├── manifest.json       ← PWA 配置
├── sw.js               ← 离线缓存
├── robots.txt          ← 防搜索引擎收录
├── data/
│   ├── words.js        ← 词库（2209 词，按难度分 18 个月）
│   ├── readers_easy.js ← 入门读物 L1-L3（30 篇）
│   ├── readers_more.js ← 进阶读物 L4-L6（30 篇）
│   └── readers.js      ← 经典读物 L4-L6（30 篇）
├── audio/
│   ├── words/          ← 单词音频，m1-m18.mp3 + index.json（Ana 美音）
│   │   └── maisie/     ← 单词音频（Maisie 英音，当前默认）
│   └── readers/
│       ├── ana/        ← 读物音频（美音童声）
│       ├── maisie/     ← 读物音频（英音小女孩）★当前默认
│       └── libby/      ← 读物音频（英音成人，KET 听力口音）
└── icons/
    ├── icon-192.png
    ├── icon-512.png
    └── icon-maskable-512.png
```

> **不需要上传**：`tools_*.js`、`tools_*.py`、`data/words_extra.js`、`data/words.bak*.js`、
> `audio/_demo/`、`_tts_tmp/`（本地开发工具/备份/样片，不是给孩子用的）

> **体积提醒**：音频合计约 60MB。GitHub 单仓库建议控制在 1GB 内，没问题；
> 但首次上传会慢。音频是**按需下载**的（用到哪个月才下哪个月的 m*.mp3），
> 孩子用起来不会卡。如果嫌上传慢，可以先不传 `audio/`，App 会自动降级到平板自带 TTS。

---

## 二、上传（网页操作，不用装 Git）

1. 右上角 **+** → **New repository**
2. 仓库名填：`ket-park`（或随意，别带孩子姓名/学校）
3. 选 **Public**（Pages 免费需要公开；工作台里没有任何个人信息，学习数据只存在平板上）
4. 勾选 **Add a README file**
5. 点 **Create repository**

进入仓库后：

6. 点 **Add file** → **Upload files**
7. 把上面 8 个文件（含 data、icons 两个文件夹）**拖进去**
   - 拖文件夹时 GitHub 会自动建立目录
   - 如果拖文件夹失败，就先点 **create new file**，在文件名框里输入 `data/words.js`（输入 `/` 会自动建目录），然后把内容粘贴进去
8. 拉到最下面，Commit message 填 `first upload`，点 **Commit changes**

---

## 三、开启 Pages

1. 仓库顶部 → **Settings** 标签
2. 左侧菜单 → **Pages**
3. **Source** 选 `Deploy from a branch`
4. **Branch** 选 `main`，右边文件夹选 `/ (root)`，点 **Save**
5. 等 1–3 分钟，刷新页面，顶部会出现绿色提示：
   ```
   Your site is live at https://你的用户名.github.io/ket-park/
   ```

**这个链接保存下来**，就是工作台的永久地址。

---

## 四、验证（PC 上先试）

浏览器打开 `https://你的用户名.github.io/ket-park/`，确认：
- 出现「英语萌宠乐园」首页
- 点「背词」能听到发音（PC 上先听一下）
- 随便点几下，无报错

---

## 五、华为平板添加到主屏

1. 平板上打开 **Microsoft Edge**（已装好的那个）
2. 地址栏输入刚才的链接，回车
3. 等页面完全加载出来（第一次慢一点，之后离线也能开）
4. 右上角 **⋮** 菜单 → **添加到手机**（或「安装应用」「添加到主屏幕」）
5. 名称改成 **英语萌宠乐园**，确认

**回到桌面，会出现一个仓鼠图标。** 点它打开：
- 没有地址栏（全屏）
- 断网也能用
- 和其他 APP 一样出现在最近任务里

### 首次打开要做的事
- 弹「允许使用麦克风」→ 选 **允许**（跟读录音要用；**不给的话跟读模块会少掉录音对比，但不影响其他功能**）
- 工作台会自动检测英文发音，检测结果显示在首页顶部

---

## 六、数据备份（重要，每周日做一次）

**学习数据存在平板浏览器里，清理缓存/清理存储空间会丢失。**

工作台已内置两种方式，家长端（密码 `1234`）里都能找到：

| 方式 | 操作 | 频率 |
|---|---|---|
| 导出备份 JSON | 家长端 → 导出备份 → 文件存到平板「下载」目录 | **每周日** |
| 导出成长相册 | 家长端 → 导出相册（HTML，可打印） | 每月闯关后 |

**恢复**：家长端 → 导入备份 → 选择之前导出的 JSON 文件。

> 建议：每月把导出的 JSON 发一份到自己的微信文件传输助手，双保险。

---

## 七、如果 GitHub Pages 打不开（国内偶尔抽风）

按下面顺序换托管，文件完全不用改：

| 备选 | 做法 | 说明 |
|---|---|---|
| **Cloudflare Pages** | 注册 cloudflare.com → Workers & Pages → Create → Pages → 连接 GitHub 选这个仓库 → 构建命令留空 → 部署 | 免费，国内通常比 GitHub 稳 |
| **腾讯云 EdgeOne Pages** | 需实名认证，国内节点最快 | 备选 |
| **本地 KSWEB** | 见下 | 完全断网可用 |

### 本地兜底（KSWEB）步骤

1. 华为应用市场搜 **KSWEB** 安装
2. 打开一次，记住它的根目录（通常是 `内部存储/htdocs`）
3. 用数据线或微信把 `ket-park` 整个文件夹复制到 `htdocs` 里
4. 打开 KSWEB → 点 **Start** → 会自动开浏览器
5. 地址栏手动输 `http://127.0.0.1:8080/ket-park/`
6. 后续每天：打开 KSWEB → 点 Start → 点收藏的链接

> 注意：localhost 也可以用麦克风，录音功能正常。缺点是要多开一个 APP。

---

## 八、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| 打开是空白页 | 文件没传全 / 路径错 | 检查 `data/` 和 `icons/` 两个文件夹是否上传成功 |
| 点了没声音 | 平板英文语音包没装 | 设置 → 辅助功能 → 文字转语音 → 下载英文语音；或启用工作台的降级模式 |
| 录音没反应 | 麦克风权限没给 | Edge 设置 → 网站设置 → 麦克风 → 允许 |
| 图标还是默认浏览器图标 | 安卓缓存 | 删掉桌面图标重新添加一次 |
| 更新了代码但平板还是旧版 | Service Worker 缓存 | 工作台首页底部有版本号，点「发现新版本」→「更新」；或 Edge 里清除该站点数据 |

---

## 九、代码更新后怎么同步

改了 `index.html` 或 `data/*.js` 之后：

1. 打开 `sw.js`，把第一行的版本号 `ket-v1` 改成 `ket-v2`（每次改都要+1，这是强制刷新的关键）
2. 重新上传改动的文件到 GitHub
3. 等 1–3 分钟
4. 平板上打开工作台 → 会提示「发现新版本」→ 点更新

> **不升版本号的话，平板会一直用旧缓存，你会以为改了没生效。**
